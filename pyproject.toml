[project]
name = "kk2-oraklet"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "pydantic",
    "python-multipart",
    "python-dotenv",
    "transformers",
    "torch",
    "pytest",
    "httpx",
]
